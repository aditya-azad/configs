---
- name: Install mise (creates ~/.local/bin/mise)
  ansible.builtin.shell: |
    set -e
    curl https://mise.run | sh
  args:
    creates: "{{ mise_bin }}"
    executable: /bin/bash
  become: true
  become_user: "{{ username }}"
