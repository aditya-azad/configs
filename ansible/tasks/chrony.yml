---
# tasks/chrony.yml

- name: Install chrony
  ansible.builtin.apt:
    name: chrony
    state: present

- name: Ensure chrony is enabled and running
  ansible.builtin.systemd:
    name: chronyd
    state: started
    enabled: true
