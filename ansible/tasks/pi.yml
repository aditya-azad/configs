---

- name: Ensure ~/.pi/agent exists
  ansible.builtin.file:
    path: "{{ home_dir }}/.pi/agent"
    state: directory
    mode: "0755"
  become: true
  become_user: "{{ username }}"

- name: Install Node.js & npm system-wide
  ansible.builtin.apt:
    name:
      - nodejs
      - npm
    state: present

- name: Ensure pi is installed (npm global, system node)
  ansible.builtin.shell: |
    set -e
    npm install -g @earendil-works/pi-coding-agent
  args:
    executable: /bin/bash
    creates: /usr/bin/pi

- name: Merge pi settings (provider/model/packages), preserving the rest
  ansible.builtin.shell: |
    set -e
    cfg="{{ home_dir }}/.pi/agent/settings.json"
    touch "$cfg"
    tmp="$(mktemp)"
    jq --arg p "openrouter" \
        --arg m "z-ai/glm-5.2" \
        '. + {
          "defaultProvider": $p,
          "defaultModel": $m,
          "packages": [
            "npm:pi-subagents",
            "npm:@zhushanwen/pi-ask-user",
            "npm:pi-agent-browser-native",
            "npm:pi-mcp-adapter",
            "npm:pi-notify",
            "npm:pi-vim"
          ]
        }' "$cfg" > "$tmp" && mv "$tmp" "$cfg"
  args:
    executable: /bin/bash
  become: true
  become_user: "{{ username }}"
  changed_when: false
