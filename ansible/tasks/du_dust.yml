---
# tasks/du_dust.yml — cargo install du-dust. Runs as the user.

- name: Install du-dust via cargo
  ansible.builtin.shell: |
    set -e
    source {{ cargo_env }}
    cargo install du-dust
  args:
    executable: /bin/bash
    creates: "{{ cargo_bin }}/dust"
  become: false
