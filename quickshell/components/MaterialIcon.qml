import QtQuick

// A glyph from the Material Design Icons font. `text` is the codepoint, e.g. "󰒓".
// Size is deliberately not defaulted: call sites use everything from 12 to 42.
StyledText {
    font.family: "Material Design Icons"
}
