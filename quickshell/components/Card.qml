import QtQuick
import qs.colors

// The standard bordered surface used for grouped content.
Rectangle {
    radius: 16
    color: Colors.surface_container
    border.width: 1
    border.color: Colors.outline_variant
}
